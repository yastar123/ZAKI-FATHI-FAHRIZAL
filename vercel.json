{
  "version": 2,
  "framework": "vite",
  "buildCommand": "pnpm --dir artifacts/portfolio run build && mkdir -p dist && cp -R artifacts/portfolio/dist/. dist/",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/assets/(.*)",
      "headers": {
        "cache-control": "public, max-age=31536000, immutable"
      }
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
